function inverter(array){
    for (obj of array.toReversed()){
        console.log(obj)
    }
}

let numeros = [1, 2, 3, 4, 5]
inverter(numeros)

let alfabeto = ['a', 'b', 'c', 'd', 'e']
inverter(alfabeto)